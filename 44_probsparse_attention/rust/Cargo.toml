[package]
name = "informer_probsparse"
version = "0.1.0"
edition = "2021"
authors = ["ML4Trading"]
description = "Informer model with ProbSparse Attention for time series forecasting using Bybit data"
license = "MIT"
readme = "README.md"

[dependencies]
# HTTP client for API requests
reqwest = { version = "0.12", features = ["json", "rustls-tls"] }

# Async runtime
tokio = { version = "1.35", features = ["full"] }

# JSON serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Numeric computing
ndarray = { version = "0.16", features = ["serde"] }
ndarray-rand = "0.15"

# Random number generation
rand = "0.9"
rand_distr = "0.5"

# Linear algebra
nalgebra = "0.33"

# Statistics
statrs = "0.17"

# Time handling
chrono = { version = "0.4", features = ["serde"] }

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# Logging
log = "0.4"
env_logger = "0.11"
tracing = "0.1"

# CSV for data export
csv = "1.3"

# Progress bars
indicatif = "0.17"

# Command line arguments
clap = { version = "4.5", features = ["derive"] }

# Async utilities
futures = "0.3"

[dev-dependencies]
tokio-test = "0.4"
approx = "0.5"

[[example]]
name = "fetch_data"
path = "examples/fetch_data.rs"

[[example]]
name = "train"
path = "examples/train.rs"

[[example]]
name = "predict"
path = "examples/predict.rs"

[[example]]
name = "backtest"
path = "examples/backtest.rs"
