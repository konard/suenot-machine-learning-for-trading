[package]
name = "hierarchical_ssm"
version = "0.1.0"
edition = "2021"
description = "Hierarchical State Space Models (HiSS) for cryptocurrency and stock trading"
license = "MIT"
authors = ["ML Trading Team"]

[lib]
name = "hierarchical_ssm"
path = "src/lib.rs"

[[example]]
name = "basic_hiss"
path = "examples/basic_hiss.rs"

[[example]]
name = "multi_scale"
path = "examples/multi_scale.rs"

[[example]]
name = "trading_strategy"
path = "examples/trading_strategy.rs"

[dependencies]
# Async runtime
tokio = { version = "1.34", features = ["full"] }

# HTTP client for Bybit API
reqwest = { version = "0.11", features = ["json"] }

# JSON serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Random number generation
rand = "0.8"
rand_distr = "0.4"

# Time handling
chrono = { version = "0.4", features = ["serde"] }

# Logging
tracing = "0.1"
tracing-subscriber = "0.3"

# Statistical functions
statrs = "0.16"
